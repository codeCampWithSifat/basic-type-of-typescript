// let emni:any ;
// emni = "Next Level Web Development Course";
// // emni = 1222 ;

// (emni as string).length  // save as <string>emni.length
// // (emni as number).toFixed()

// function kgToGram(param: string | number): string | number | undefined {
//   if (typeof param === "string") {
//     const value = parseInt(param) * 1000;
//     return `The Converted Result Is ${value} gram`;
//   }
//   if (typeof param === "number") {
//     const value = param * 1000;
//     return value;
//   } else {

//   }
// };

// const resultToBeNumber = kgToGram(20) as number ;
// const resultToBeNumber1 = <string> kgToGram("200"); // same as kgToGram(20) as string ;


// type CustomeError = {message : string}

// try {

// } catch(err) {
//     console.log((err as CustomeError).message)
// }